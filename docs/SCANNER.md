# Scanner

The first scanner implementation uses browser camera capture, local canvas-based visual filters, a multi-page capture list, and local PDF generation. The document is saved into the active local storage provider with a timestamped name and a scan source flag.

Manual four-corner adjustment, automatic edge detection, perspective correction, page reorder, and searchable-PDF generation are future scanner milestones. They are not represented as completed functionality in the first release.
