/** Field Desk understanding layer: deterministic extraction only; it never invents document facts. */
export interface ExtractionResult { dates: string[]; currency: string[]; emails: string[]; phones: string[]; iban: string[]; references: string[]; }
const unique = (values: string[]) => Array.from(new Set(values.map((value) => value.trim()).filter(Boolean))).slice(0, 8);
export function extractDocumentFacts(text: string): ExtractionResult { const datePattern = /\b(?:\d{1,2}[\/.\-]\d{1,2}[\/.\-]\d{2,4}|\d{1,2}\s+(?:January|February|March|April|May|June|July|August|September|October|November|December|gennaio|febbraio|marzo|aprile|maggio|giugno|luglio|agosto|settembre|ottobre|novembre|dicembre)\s+\d{4})\b/gi; const currencyPattern = /(?:€\s?\d[\d.,]*|\b\d[\d.,]*\s?(?:EUR|USD|GBP|€|\$|£))\b/gi; const emailPattern = /\b[A-Z0-9._%+-]+@[A-Z0-9.-]+\.[A-Z]{2,}\b/gi; const phonePattern = /(?:\+?\d{1,3}[ .-]?)?(?:\(?\d{2,4}\)?[ .-]?)?\d{3,4}[ .-]\d{3,4}\b/g; const ibanPattern = /\b[A-Z]{2}\d{2}(?:[ ]?[A-Z0-9]){11,30}\b/g; const referencePattern = /\b(?:ref(?:erence)?\s*(?:no\.?|number)?|protocollo|pratica|codice)\s*[:#-]?\s*[A-Z0-9][A-Z0-9\-/]{3,}\b/gi; return { dates: unique(text.match(datePattern) ?? []), currency: unique(text.match(currencyPattern) ?? []), emails: unique(text.match(emailPattern) ?? []), phones: unique(text.match(phonePattern) ?? []), iban: unique(text.match(ibanPattern) ?? []), references: unique(text.match(referencePattern) ?? []) }; }
